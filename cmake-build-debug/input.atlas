DEFINE doMath(x:NUM, y:NUM,) $ NUM {
    NUM answer = x + y;
    RETURN answer;
};

DEFINE sayHi() $ STRING {
    STRING msg = "Hi!";
    RETURN msg;
};

CC i want to be able to use <msg> instead of <messg>
DEFINE sayHello() $ STRING {

    STRING messg = "Hello!";
    RETURN messg;
};


@doMath(1,2,);
@sayHi();

WRITE @sayHi();

STRING msg = @sayHello();

WRITE msg;
